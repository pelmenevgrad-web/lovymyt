-- ============================================================
-- Мінімальна тривалість заходу (для відображення).
-- Максимальна тривалість вже пишеться в існуючу колонку events.end_time
-- (start_time + максимум годин) — вона й визначає автозавершення.
-- ============================================================

ALTER TABLE events ADD COLUMN IF NOT EXISTS duration_min_hours numeric(4,1);
