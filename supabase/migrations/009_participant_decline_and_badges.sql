-- ============================================================
-- Організатор може відмовити учаснику з описом причини, і після
-- заходу учасники можуть давати одне одному жартівливі "статуси".
-- ============================================================

ALTER TABLE event_participants ADD COLUMN decline_reason text;
ALTER TABLE reviews ADD COLUMN funny_status text;
