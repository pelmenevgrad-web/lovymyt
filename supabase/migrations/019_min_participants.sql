-- ============================================================
-- Мінімум учасників тепер реально щось означає: за годину до
-- старту попереджаємо організатора й учасників, якщо набір
-- неповний, а організатор може явно підтвердити "продовжити".
-- ============================================================

ALTER TABLE events ADD COLUMN force_continue boolean DEFAULT false;
ALTER TABLE events ADD COLUMN low_turnout_notified_at timestamptz;
