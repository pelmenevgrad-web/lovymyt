-- ============================================================
-- Вейтлист для заходів: коли max_participants (або гендерна
-- квота) заповнено, приєднання більше не відхиляється намертво —
-- людина стає 'waitlisted' і автоматично підвищується до
-- 'accepted', щойно звільниться місце.
--
-- ВАЖЛИВО: цей файл треба виконати ОКРЕМО і ПЕРШИМ, до
-- 024_waitlist_functions.sql — Postgres не дозволяє
-- використовувати нове значення enum у тій самій транзакції, де
-- воно було додане.
-- ============================================================

ALTER TYPE participant_status_enum ADD VALUE IF NOT EXISTS 'waitlisted';
