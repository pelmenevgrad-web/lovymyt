-- ============================================================
-- Перейменування "карми" на "Хвилі" (user-facing назва валюти
-- реферальної програми) — приводимо схему у відповідність з новою
-- назвою, щоб не тримати в БД термін, якого більше немає в продукті.
-- ============================================================

ALTER TABLE users RENAME COLUMN karma_points TO wave_points;
ALTER FUNCTION credit_karma_balance(uuid, int) RENAME TO credit_wave_balance;
